plugins {
    alias(libs.plugins.kotlin.jvm)
    `java-library`
}

dependencies {
    // No dependencies - just test fixtures
}

// Note: This module is not published - it's only for internal testing
